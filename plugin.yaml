name: keepkeys
version: "0.6.0"
description: Native-vault secrets from the host or a private Tailscale phone page, with approval-gated agent use and no plaintext retrieval.
author: BarnLabs
provides_tools:
  - keepkeys_store
  - keepkeys_store_from_phone
  - keepkeys_list
  - keepkeys_rotate
  - keepkeys_revoke
  - keepkeys_remove
  - keepkeys_run
  - keepkeys_status
  - keepkeys_doctor
