name: keepkeys
version: "0.4.2"
description: Explicit paste-to-vault secrets for approval-gated agent commands on macOS, Windows, and Linux, without plaintext retrieval.
author: BarnLabs
provides_tools:
  - keepkeys_store
  - keepkeys_list
  - keepkeys_remove
  - keepkeys_run
  - keepkeys_status
  - keepkeys_doctor
