{
  "name": "keepkeys",
  "version": "0.4.0",
  "description": "Local native-vault secrets for approval-gated agent commands on macOS, Windows, and Linux, without plaintext retrieval.",
  "mcpServers": {
    "keepkeys": {
      "command": "node",
      "args": [
        "${extensionPath}${/}plugins${/}keepkeys${/}mcp${/}server.mjs",
        "--stdio"
      ],
      "cwd": "${extensionPath}${/}plugins${/}keepkeys"
    }
  }
}
