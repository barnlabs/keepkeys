#!/bin/sh
set -eu

root=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)

KEEPKEYS_PORTAL_TAILNET_TEST=1 \
  exec node \
    "$root/plugins/keepkeys/scripts/keepkeys-portal.mjs" \
    --tailnet-portal-self-test
